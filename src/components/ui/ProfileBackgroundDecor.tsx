import React from 'react';

/**
 * Profile Background Decor - Disabled
 * Decorative elements have been removed as per user request.
 * Component returns null to maintain backwards compatibility.
 */
export const ProfileBackgroundDecor: React.FC = () => {
    return null;
};
